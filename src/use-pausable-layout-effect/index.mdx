---
category: Lifecycle
features: ['Pausable', 'LowLevel']
---

# usePausableLayoutEffect

import { HooksType } from '@/components'

<HooksType {...frontmatter} />

A React Hook like [React.useLayoutEffect](https://react.dev/reference/react/useLayoutEffect), but pausable.

## Demo

import { App } from './demo'

<App />

## Usage



## Source

import { Source } from '@/components'

<Source />

## API

```tsx
const pausable = usePausableLayoutEffect(callback, deps)
```

### Callback

Same as [React.useLayoutEffect](https://react.dev/reference/react/useLayoutEffect).

### Deps

Same as [React.useLayoutEffect](https://react.dev/reference/react/useLayoutEffect).

### Returns

import { Tip } from '@/components'

<Tip type="pausable" />

A `Pausable` instance.

---
category: Lifecycle
features: ['Pausable', 'LowLevel']
---

# usePausableEffect

import { HooksType } from '@/components'

<HooksType {...frontmatter} />

A React Hook like [React.useEffect](https://react.dev/reference/react/useEffect), but pausable.

## Demo

Try to increase the dep counter when effect in different states.

import { App } from './demo'

<App />

## Usage

See API for more details.

## Source

import { Source } from '@/components'

<Source />

## API

```tsx
const pausable = usePausableEffect(callback, deps)
```

### Callback

Same as [React.useEffect](https://react.dev/reference/react/useEffect).

### Deps

Same as [React.useEffect](https://react.dev/reference/react/useEffect).

### Returns

import { Tip } from '@/components'

<Tip type="pausable" />

A `Pausable` instance.

# useKeyStatus

import { HooksType } from '@/components'

<HooksType category="Sensors" />

这是一个 React Hook，用于跟踪按键的按下状态。

## 演示 Demo \{#demo}

尝试按 `-`/`=` 键以检查键的状态。

import { App } from './demo'

<App />

## 用法 Usage \{#usage}

import { RawCode } from '@/components'
import source from '!!raw-loader!./demo'

<RawCode>{source}</RawCode>

## 源码 Source \{#source}

import { Source } from '@/components'

<Source />

## API \{#api}

```tsx
const isPressed = useKeyStatus(keyFilter, handler, options)
```

### 键过滤器 KeyFilter \{#key-filter}

import Link from '@docusaurus/Link'

请参见 `useKeyStroke` 的 <Link to="/reference/use-key-stroke/#key-filter">键过滤器 KeyFilter</Link> 了解更多详细信息。

### 处理函数 Handler \{#handler}

```tsx
export type UseKeyStatusHandler = UseKeyStrokeHandler
```

请参见 `useKeyStroke` 的 <Link to="/reference/use-key-stroke/#handler">处理函数 Handler</Link> 了解更多详细信息。

### 选项 Options \{#options}

```tsx
export type UseKeyStatusOptions = Omit<UseKeyStrokeOptions, 'eventName' | 'dedupe'>
```

请参见 `useKeyStroke` 的 <Link to="/reference/use-key-stroke/#options">选项 Options</Link> 了解更多详细信息。

### 返回值 Returns \{#returns}

一个 `boolean` 值，表示按键的按下状态。

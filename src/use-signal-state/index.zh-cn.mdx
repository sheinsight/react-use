# useSignalState

import { HooksType } from '@/components'

<HooksType category={frontmatter.category} />

一个类似于 [Solid](https://www.solidjs.com) 中 `createSignal` 的 React 钩子，**没有闭包问题**。

## 演示 Demo \{#demo}

尝试快速改变下面两种类型的状态，然后在 DevTools 中查看日志。

import { App } from './demo'

<App />

## 用法 Usage \{#usage}



## 源码 Source \{#source}

import { Source } from '@/components'

<Source />

## API \{#api}

```tsx
const [state, setState] = useSignalState(initialState)
```

### 初始状态 InitialState \{#initial-state}

与 [React.useState](https://react.dev/reference/react/useState) 相同。

### 选项 Options \{#options}

更多详情见 [useSafeState#options](/reference/use-safe-state/#options)。

### 返回值 Returns \{#returns}

```tsx
export type UseSignalStateReturns = [() => T, ReactSetState<T>]
```

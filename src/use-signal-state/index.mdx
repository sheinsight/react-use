---
category: State
---

# useSignalState

import { HooksType } from '@/components'

<HooksType category="State" />

A React Hook that use state like `createSignal` of [Solid](https://www.solidjs.com), **without closure issues**.

## Demo

Try to change the two type of state below quickly, and then see the log in DevTools.

import { App } from './demo'

<App />

## Usage



## Source

import { Source } from '@/components'

<Source />

## API

```tsx
const [state, setState] = useSignalState(initialState)
```

### InitialState

Same as [React.useState](https://react.dev/reference/react/useState).

### Options

See [useSafeState#options](/reference/use-safe-state/#options) for more details.

### Returns

```tsx
export type UseSignalStateReturns = [() => T, ReactSetState<T>]
```
